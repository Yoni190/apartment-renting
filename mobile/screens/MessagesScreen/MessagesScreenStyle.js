import { StyleSheet, Platform } from 'react-native'
import { colors, spacing, radius, shadows, typography } from '../../theme'

export default StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  kb: { flex: 1 },
  listContent: { paddingHorizontal: spacing.md, paddingBottom: Platform.OS === 'android' ? 180 : 140, paddingTop: spacing.md},

  // conversation list
  convoRow: { flexDirection: 'row', paddingVertical: spacing.md, alignItems: 'center', borderBottomWidth: 1, borderBottomColor: colors.border },
  avatarPlaceholder: { width: 48, height: 48, borderRadius: 24, backgroundColor: colors.primaryLight, alignItems: 'center', justifyContent: 'center' },
  avatarText: { color: colors.primary, fontWeight: '700' },
  convoName: { fontSize: 16, fontWeight: '700', color: colors.textPrimary },
  convoTime: { fontSize: 12, color: colors.textMuted },
  convoPreview: { color: colors.textSecondary, flex: 1, marginRight: spacing.sm },
  unreadBadge: { backgroundColor: colors.primary, minWidth: 24, height: 24, borderRadius: 12, alignItems: 'center', justifyContent: 'center', paddingHorizontal: spacing.xs },
  unreadText: { color: colors.white, fontSize: 12, fontWeight: '700' },

  // empty state
  emptyContainer: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  emptyTitle: { fontSize: 18, color: colors.textSecondary, fontWeight: '600' },

  // floating new message button
  fab: { position: 'absolute', right: 18, bottom: 24, width: 56, height: 56, borderRadius: 28, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center', elevation: 4 },

  messageRow: {
    marginVertical: spacing.xs,
    flexDirection: 'row',
    paddingHorizontal: spacing.sm,
  },
  messageRowSent: { justifyContent: 'flex-end' },
  messageRowReceived: { justifyContent: 'flex-start' },

  bubble: {
    maxWidth: '78%',
    paddingVertical: spacing.sm,
    paddingHorizontal: spacing.md,
    borderRadius: 18,
  },
  bubbleSent: {
    backgroundColor: colors.primary,
    borderTopRightRadius: spacing.xs,
    borderTopLeftRadius: 18,
    borderBottomLeftRadius: 18,
    borderBottomRightRadius: spacing.xs,
  },
  bubbleReceived: {
    backgroundColor: colors.white,
    borderTopLeftRadius: 6,
    borderTopRightRadius: 18,
    borderBottomLeftRadius: 6,
    borderBottomRightRadius: 18,
    borderWidth: 1,
    borderColor: colors.border,
  },
  messageText: {
    fontSize: 16,
    lineHeight: 20,
  },
  messageTextSent: { color: colors.white },
  messageTextReceived: { color: colors.textPrimary },

  timeText: {
    fontSize: 11,
    color: colors.textMuted,
    marginTop: spacing.xs,
    textAlign: 'right',
  },

  // avatar in message rows
  avatarSmall: { width: 32, height: 32, borderRadius: 16, backgroundColor: colors.primaryLight, alignItems: 'center', justifyContent: 'center', marginRight: spacing.sm },
  avatarSmallText: { color: colors.primary, fontWeight: '700' },

  // spacer used to push sent messages further to the right edge
  sentSpacer: { width: 2 },

  inputRow: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: Platform.OS === 'android' ? 36 : 12,
    flexDirection: 'column',
    alignItems: 'stretch',
    paddingHorizontal: 14,
    paddingVertical: Platform.OS === 'android' ? 12 : spacing.md,
    borderTopWidth: 1,
    borderTopColor: colors.border,
    backgroundColor: colors.white,
    shadowColor: colors.black,
    shadowOffset: { width: 0, height: -1 },
    shadowOpacity: 0.05,
    shadowRadius: 4,
    elevation: 6,
  },
  inputInner: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  input: {
    flex: 1,
    minHeight: 40,
    maxHeight: 120,
    paddingHorizontal: spacing.md,
    paddingVertical: Platform.OS === 'android' ? 12 : spacing.sm,
    backgroundColor: colors.background,
    borderRadius: spacing.xl,
    marginRight: spacing.sm,
  },
  templateBtn: {
    width: 30,
    height: 30,
    borderRadius: radius.sm,
    backgroundColor: colors.border,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: spacing.xs,
  },
  sendBtn: {
    width: 44,
    height: 44,
    borderRadius: 22,
    backgroundColor: colors.primary,
    alignItems: 'center',
    justifyContent: 'center',
  },
  mediaBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: colors.border,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: spacing.sm,
  },
  mediaBtnText: {
    fontSize: 20,
    color: colors.textPrimary,
    fontWeight: '700',
    marginTop: -2,
  },
  mediaOption: {
    paddingVertical: spacing.md,
  },
  mediaOptionText: {
    fontSize: 16,
    fontWeight: '600',
    color: colors.textPrimary,
  },
  scrollToBottomBtn: {
    position: 'absolute',
    right: spacing.lg,
    width: 44,
    height: 44,
    borderRadius: 22,
    backgroundColor: colors.textMuted,
    alignItems: 'center',
    justifyContent: 'center',
    ...shadows.sm,
  },
  mediaContainer: {
    width: 220,
    height: 140,
    borderRadius: radius.md,
    overflow: 'hidden',
    marginBottom: spacing.xs,
    backgroundColor: colors.textPrimary,
  },
  mediaImage: {
    width: '100%',
    height: '100%',
  },
  mediaPlaceholder: {
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: colors.navBg,
  },
  mediaLoadingOverlay: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 0,
    bottom: 0,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(0,0,0,0.35)',
  },
  viewerOverlay: {
    flex: 1,
    backgroundColor: colors.black,
  },
  viewerClose: {
    position: 'absolute',
    top: 40,
    left: spacing.lg,
    zIndex: 2,
    width: 36,
    height: 36,
    borderRadius: 18,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(255,255,255,0.12)',
  },
  viewerBody: {
    flex: 1,
    paddingTop: 64,
  },
  viewerScrollContent: {
    flexGrow: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  viewerImage: {
    width: '100%',
    height: '100%',
  },
  templateOverlay: {
    flex: 1,
    justifyContent: 'flex-end',
    backgroundColor: 'rgba(0,0,0,0.35)',
  },
  templateCard: {
    backgroundColor: colors.white,
    padding: spacing.lg,
    borderTopLeftRadius: radius.md,
    borderTopRightRadius: radius.md,
  },
  templateFormCard: {
    backgroundColor: colors.white,
    padding: spacing.md,
    borderTopLeftRadius: radius.md,
    borderTopRightRadius: radius.md,
    minHeight: 240,
  },
  templateFormScroll: {
    maxHeight: 360,
  },
  templateFormContent: {
    paddingBottom: spacing.xs,
  },
  templateTitle: {
    fontSize: 15,
    fontWeight: '700',
    marginBottom: spacing.xs,
    color: colors.textPrimary,
  },
  templateFormInput: {
    backgroundColor: colors.primaryLight,
    borderRadius: radius.sm,
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.sm,
    minHeight: 140,
    fontSize: 13,
    color: colors.textPrimary,
  },
  templateFieldRow: {
    marginBottom: spacing.xs,
  },
  templateFieldLabel: {
    fontSize: 11,
    color: colors.textSecondary,
    marginBottom: 2,
  },
  templateFieldLabelOnDark: {
    color: colors.textPrimary,
    fontWeight: '700',
  },
  templateFieldInput: {
    backgroundColor: colors.primaryLight,
    borderRadius: radius.sm,
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.sm,
    fontSize: 13,
    color: colors.textPrimary,
  },
  templateFieldInputMultiline: {
    minHeight: 64,
  },
  templateRadioGroup: {
    marginTop: spacing.xs,
    marginBottom: spacing.xs,
    gap: spacing.xs,
  },
  templateRadioRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  templateRadioOuter: {
    width: 18,
    height: 18,
    borderRadius: 9,
    borderWidth: 2,
    borderColor: colors.textMuted,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: spacing.sm,
    backgroundColor: colors.white,
  },
  templateRadioInner: {
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: colors.primary,
  },
  templateRadioLabel: {
    fontSize: 12,
    color: colors.textPrimary,
  },
  templateRadioOtherInput: {
    marginTop: spacing.xs,
  },
  templateMessageCard: {
    backgroundColor: 'rgba(255,255,255,0.16)',
    borderRadius: radius.sm,
    padding: spacing.sm,
    marginBottom: spacing.xs,
  },
  templateMessageSubject: {
    fontSize: 13,
    fontWeight: '700',
    color: colors.white,
    marginBottom: spacing.xs,
  },
  templateMessageRow: {
    marginBottom: spacing.xs,
  },
  templateMessageLabel: {
    fontSize: 10,
    color: colors.textMuted,
    marginBottom: 2,
  },
  templateMessageValue: {
    fontSize: 12,
    color: colors.white,
  },
  templateFormActions: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    alignItems: 'center',
    marginTop: spacing.md,
    gap: spacing.md,
  },
  templateFormInsert: {
    backgroundColor: colors.primary,
    paddingVertical: spacing.sm,
    paddingHorizontal: 14,
    borderRadius: radius.sm,
  },
  templateFormInsertText: {
    color: colors.white,
    fontWeight: '700',
  },
  templateOption: {
    paddingVertical: spacing.md,
  },
  templateOptionText: {
    fontSize: 16,
    fontWeight: '600',
    color: colors.textPrimary,
  },
  templateCancel: {
    marginTop: spacing.sm,
    alignItems: 'flex-end',
  },
  templateCancelText: {
    color: colors.textSecondary,
    fontWeight: '600',
  },
  templatePreview: {
    backgroundColor: colors.background,
    borderRadius: radius.md,
    paddingVertical: spacing.sm,
    paddingHorizontal: spacing.md,
    marginBottom: spacing.sm,
    borderLeftWidth: 3,
    borderLeftColor: colors.primary,
    flexDirection: 'row',
    alignItems: 'center',
  },
  templatePreviewLeft: {
    flex: 1,
  },
  templatePreviewTitle: {
    fontSize: 12,
    fontWeight: '700',
    color: colors.textPrimary,
  },
  templatePreviewText: {
    fontSize: 12,
    color: colors.textSecondary,
    marginTop: 2,
  },
  templatePreviewInsert: {
    paddingVertical: spacing.xs,
    paddingHorizontal: spacing.md,
    backgroundColor: colors.primary,
    borderRadius: radius.sm,
    marginLeft: spacing.sm,
  },
  templatePreviewInsertText: {
    color: colors.white,
    fontWeight: '700',
    fontSize: 12,
  },
  templatePreviewClear: {
    paddingVertical: spacing.xs,
    paddingHorizontal: spacing.sm,
    marginLeft: spacing.xs,
  },
  templatePreviewClearText: {
    color: colors.textSecondary,
    fontWeight: '700',
    fontSize: 16,
  },
  replyComposer: {
    backgroundColor: colors.background,
    borderRadius: radius.md,
    paddingVertical: spacing.sm,
    paddingHorizontal: spacing.md,
    marginBottom: spacing.sm,
    borderLeftWidth: 3,
    borderLeftColor: colors.primary,
    flexDirection: 'row',
    alignItems: 'center',
  },
  replyComposerLeft: {
    flex: 1,
  },
  replyComposerTitle: {
    fontSize: 12,
    fontWeight: '700',
    color: colors.textPrimary,
  },
  replyComposerText: {
    fontSize: 12,
    color: colors.textSecondary,
    marginTop: 2,
  },
  replyComposerClose: {
    padding: spacing.xs,
  },
  replySnippet: {
    paddingVertical: spacing.xs,
    paddingHorizontal: spacing.sm,
    borderRadius: radius.sm,
    marginBottom: spacing.xs,
    borderLeftWidth: 3,
  },
  replySnippetSent: {
    backgroundColor: 'rgba(255,255,255,0.15)',
    borderLeftColor: colors.primaryLight,
  },
  replySnippetReceived: {
    backgroundColor: colors.background,
    borderLeftColor: colors.textMuted,
  },
  replyName: {
    fontSize: 11,
    fontWeight: '700',
  },
  replyNameSent: {
    color: colors.primaryLight,
  },
  replyNameReceived: {
    color: colors.textPrimary,
  },
  replyText: {
    fontSize: 11,
    marginTop: 2,
  },
  replyTextSent: {
    color: colors.textMuted,
  },
  replyTextReceived: {
    color: colors.textSecondary,
  },
  // compact conversation header (Telegram-like)
  compactHeader: {
    height: 64,
    paddingTop: spacing.md,
    paddingHorizontal: spacing.md,
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.white,
    borderBottomWidth: 1,
    borderBottomColor: colors.border,
  },
  headerLeft: {
    width: 48,
    justifyContent: 'center',
    alignItems: 'flex-start',
  },
  backIcon: {
    fontSize: 26,
    color: colors.textPrimary,
    fontWeight: '700',
  },
  headerCenter: {
    flex: 1,
    justifyContent: 'center',
    paddingLeft: spacing.sm,
  },
  headerTitle: {
    fontSize: 16,
    fontWeight: '700',
    color: colors.textPrimary,
  },
  headerRight: {
    width: 48,
    alignItems: 'flex-end',
  },
  // custom chat header (dark blue, avatar, back icon)
  chatHeader: {
    height: 96,
    paddingTop: 22,
    paddingHorizontal: spacing.md,
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.primary,
    borderBottomWidth: 1,
    borderBottomColor: colors.primaryDark,
  },
  chatHeaderLeft: {
    width: 48,
    justifyContent: 'center',
    alignItems: 'flex-start',
  },
  chatBackContainer: {
    width: 40,
    height: 40,
    borderRadius: spacing.xl,
    backgroundColor: 'rgba(255,255,255,0.12)',
    alignItems: 'center',
    justifyContent: 'center',
    ...shadows.sm,
  },
  chatBack: {
    fontSize: 22,
    color: colors.white,
    fontWeight: '700',
  },
  chatHeaderCenter: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    paddingLeft: spacing.xs,
  },
  chatAvatar: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: colors.primaryDark,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: spacing.md,
  },
  chatAvatarText: {
    color: colors.white,
    fontWeight: '700',
  },
  chatHeaderTitle: {
    color: colors.white,
    fontSize: 16,
    fontWeight: '700',
    maxWidth: '70%'
  },
  chatHeaderRight: { width: 48 },
})
